extends default

block content
  p Para cualquier consulta, puedes mandar un correo a:
  p: a(href=`mailto:contacto@${websiteDomain}`) contacto@#{websiteDomain}

block footer
  a(href='/') ← volver al inicio
