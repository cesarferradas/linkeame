extends default

block content
  if msg
    p.error #{msg}
  form(method='POST')
    input(name='_csrf' type='hidden' value=csrfToken)
    input(name='_numbers' type='hidden' value=numbers)
    p Enlace largo:
    input#url(name='url' required type='text')
    p Codigo personalizado (opcional):
    code https://#{websiteDomain}/
    input(name='code' type='text')
    p Verificación:
    img(src=`data:image/jpeg;base64,${validationCode}`)
    input#challenge(name='challenge' required type='text')
    p
    button(type='submit') Acortar
